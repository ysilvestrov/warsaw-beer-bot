# Дизайн — ідентичність цифр назви: різні цифри з обох боків означають різні пива (закриває #636)

**Issue:** #636 (окремий випадок — #621). **Статус:** дизайн погоджено з користувачем 2026-09-16 (брейншторм у сесії);
**асиметрію ролей** («число лише в кандидата — запасний рівень») погоджено того ж дня після рев'ю ядра, див. «Асиметрія ролей».
Правило для градусів **переглянуто посеред брейншторму**: перша узгоджена версія («градус — ідентичність, коли назва
без нього — голий бренд») спростована прототипом на прод-даних, див. «Спростовані варіанти».

Докази (скрипти проб і прототипу, їхній вивід) — поза git, у `~/warsaw-beer-probes/636/`.

## Проблема (доведена)

### Механізм

`normalizeName` відкидає цифрові токени (`isNumericNoise`), а `stripSearchNoise` перед ним викидає дужки разом із
роками та градуси разом із числом. Нормалізований ключ читають як **ідентичність** у п'яти місцях, і лише одне з них
дивиться на цифри:

| місце | що робить з ключем | цифри сьогодні |
|---|---|---|
| `matchPrepared`, точна стадія (обидві гілки: звичайна і split-invariant #169) | усі рядки з рівним ключем — «exact», перемагає ABV, далі найновіший `id` | лише 4-значний рік (`extractYear`): `yearMatch` / `noYear` / `wrongYear` |
| `matchPrepared`, fuzzy-стадія | найкращий результат пошуку `seedBrewery + nn` | ні |
| `ensureBeerRow` (`/enrich/*`) | `findBeerByNormalized(...).get()` — перший рядок пари | ні |
| `ensureOrphan` / `resolvableOrphan` (`upsertBeerByBid`) | сирота з рівною парою | так — `numericTokensCompatible` (#617) |
| `lookupBeer` (гейт пошуку Untappd) | стадії порівнюють нормалізовані назви кандидатів Algolia | лише гілка `hasExactBrandRemainder` |

### Доказ (2026-09-16, прод-БД лише читання + живий Algolia)

**Точна стадія — живі хибні лінки.** З 1 494 автоматичних точних лінків кранів (`confidence 1`, не пін, без
`merged_at`) цифри (`numericNameTokens`) розходяться з обох боків у 14. Справді хибні: `Juicy Trap #19 → #20`,
`Dr.Hazy #4/#7 → #5`, `Przetwór #4 → #3`, `SPECIMEN 002 → 001`, `Black Celebration #4 → #3`,
`La Tomatina Vol.4 → vol.3`, `Konrad 12 → Svetlé Výčepní 10`, `Stary Sad 2025 → 2023`, `11 Horky → 12 Horky`. Таблиця
в issue (56) міряна до v33 (#632), яка частину тих лінків прибрала; `Przekład #9` (#621) сьогодні вказує на свій рядок.

**Fuzzy-стадія перелінкує, коли точна відмовить** (`probe636-fuzzy.ts`: fuzzy-стадія відтворена як у `matchPrepared`
при порожніх `exacts`, повний прод-каталог). 10 з 14 кранів fuzzy знаходить **той самий** рядок чужого номера зі
скором 1.000 — ключі рівні. Решту рятує лише інша броварня рядка.

**Гейт пошуку сліпий до цифр** (`probe636-lookup.ts`, живий Algolia, канарка зелена):
`Piwne Podziemie / Dr.Hazy #7` → **`Dr. Hazy #4`** [5899401]; `Dr.Hazy #97` (такого пива немає) → **теж `#4`**.
`SPECIMEN 002` → `Specimen 002` (правильно), `SPECIMEN 097` / `Przetwór #94` → not_found (Algolia AND-ить число).

**`ensureBeerRow` віддає рядок чужого номера** (`probe636-ensure.ts`). 468 з 598 нормалізованих пар з кількома
рядками віддають картці рядок з іншими цифрами, і в усіх 468 цей рядок злінкований. Приклад: картка
`Trappistes Rochefort 10` отримує `Trappistes Rochefort 6` [10722] — рядок злінкований, тож пошук для картки не
запускається, а опублікований крамницею bid десятки піде в репарацію #384 і перелінкує рядок шістки. Запит `/enrich`
у журнал не пишеться, тож кількість уже реалізованих випадків невідома — доказ структурний.

### Ціна — видима користувачу

Паби й картки крамниць показують **чуже пиво того самого ряду** — з його рейтингом і статусом «пив» (`Juicy Trap #19`
позначено випитим, бо випито `#20`). Через репарацію #384 і пам'ять злиття (#614) хибний лінк може стати тривким.

## Рішення

**Одна функція ідентичності цифр назви, застосована в усіх п'яти місцях.** Новий модуль
`src/domain/digit-identity.ts` (не `name-identity.ts` — це ім'я належить #505: `nameIdentity`, `identityAllowsApprox`):

- `readNameDigits(name: string): NameDigits` — читає цифри **сирої** назви один раз;
- `digitIdentity(input: NameDigits, candidate: NameDigits): 'same' | 'year-fallback' | 'number-fallback' | 'different'` —
  **ролі не симетричні**: `input` — текст, який матчимо (кран, картка крамниці), `candidate` — рядок, яким він може бути
  (рядок каталогу, результат пошуку Untappd).

`numericTokensCompatible` видаляється; `normalizeName`, `normalized_name` у БД і запити до Algolia **не змінюються**
(міграції немає).

### Читання цифр (`readNameDigits`)

Порядок кроків важливий: кожен крок прибирає те, що наступний прочитав би хибно.

1. **ABV у будь-якому записі викидається:** `5%`, `5,5%`, `4'8%` (апостроф як десятковий), `<0,5%`, `5.3 abv`.
2. **Градус** — `N°`, `N*`, разом із хвостом `°·5%` — вилучається в окремий список `grades` (м'які, див. нижче).
3. **Рік з апострофом:** `'26` / `26'` → `2026`, зокрема приклеєний до слова (`Hoppiness'26`, `Kyiv'25`). Голе `26`
   лишається числом.
4. **Цифрові токени** — цифрова послідовність, **не приклеєна до букви чи цифри** з обох боків (`#7`, `AB:20`,
   `no.5`, `vol.01`, `(2018)`, `2015-2022`). Крапка/кома перед числом не блокує читання, якщо перед нею не цифра
   (`vol.01` читається, `10.5` — один токен). Десяткове число береться цілим або ніяким: обʼєм, приклеєний до
   одиниці (`0,5l`, `1.5L`), не дає жодного числа (без цього regex відкочувався всередину і читав `0`). Далі:
   - десяткова частина лишається частиною токена (`2.0`, `10,5` → `10.5`); префікс `v` знімається (`v1.0` → `1.0`);
   - порядковий суфікс знімається (`10th` → `10`);
   - провідні нулі знімаються (`002` → `2`);
   - цифри, приклеєні до слова (`WFP10`, `TAP04`, `BA23.03`), **не читаються**.
5. **Класифікація токена:**
   - `19xx`/`20xx` → `years` (множина; діапазон `2015-2022` дає обидва);
   - `N.0` → `versions`;
   - ціле 8–14 **без маркера** (`#`, `no.`, `nr.`, `vol.`, `batch`, `edition`, `part` безпосередньо перед ним, провідний
     нуль, префікс `v`) → `soft` (чеська десятка/дванадцятка без знака `°`: `Svijanský Máz 11`);
   - решта → `numbers` (мультимножина).

### Правило (`digitIdentity(input, candidate)`)

1. **Числа входу — жорсткі.** Кожне число `numbers` входу мусить знайти пару серед чисел кандидата або бути **покритим**
   його градусом чи м'яким числом (`Otakar 11°` ↔ `Otakar 11`). Непокрите число входу → `different`
   (`Funky Monkey #2` → `Funky Monkey`, `Kronenbourg 1664` → `Kronenbourg`).
2. **Число лише в кандидата — запасний рівень `number-fallback`** (`Cucumber Gose` → `10th Anniversary #6: Cucumber
   Gose`, `Few More Beers` → `Few More Beer 004/108`), **якщо** вхід не має власного непокритого числа іншого роду:
   м'якого (`Trappistes Rochefort 10` → `Trappistes Rochefort 6` — `different`) чи версії (`Potion #2.0` → `Potion #18` —
   `different`). Градуси входу в цю перевірку не входять — це екстракт (`11° Blackberry Gose` → `Sourberry #4 Blackberry
   Gose` — `number-fallback`).
3. **Градуси й м'які числа ніколи самі не розводять** (`Pils 12°` ↔ `Pils`, `Białe IPA 16°` ↔ `Białe IPA 14°`) —
   з двома винятками, де вони є єдиним носієм номера:
   - м'які числа з обох боків і різні → `different`;
   - м'яке число з одного боку, градус з іншого, жоден з м'яких не дорівнює жодному градусу, а з боку градуса м'яких
     немає → `different` (`KONRAD 12°` ↔ `Konrad Svetlé Výčepní 10`).
4. **Версії** (`N.0`): з обох боків і різні → `different` (`Ambrosia 9.0` ↔ `5.0`); з одного боку — не розводять
   (`SPOKO CYDR 2.0` ↔ `Spoko Cydr Zweigelt Edition`).
5. **Роки:** з обох боків — множини рівні, інакше `different` (`Autonomia (2021/2022)` ≠ `(2022/2023)`), і конфлікт
   років переважає `number-fallback` (`Abraxas 2024` → `Abraxas (Batch 7) 2025` — `different`).
6. **Рівень:** `different`, якщо спрацювало будь-що вище; інакше `number-fallback`, якщо в кандидата є число, якого
   немає у входу (навіть коли рік лише з одного боку — номерний рівень слабший); інакше `year-fallback`, якщо рік лише з
   одного боку; інакше `same`.

`year-fallback` і `number-fallback` — не «те саме пиво», а «прийнятно, якщо кращого немає», у порядку
`same` > `year-fallback` > `number-fallback`. `year-fallback` — нинішня семантика `noYear` у матчері й років у
`numericTokensCompatible`.

### Асиметрія ролей (погоджено після рев'ю ядра, 2026-09-16)

Перша погоджена версія була симетричною: число лише з одного боку — `different` в обидва боки. Рев'ю ядра знайшло
`Few More Beers 19°` → `Few More Beer 004/108` (правильний fuzzy-лінк, який би загубився), користувач перевірив на
Untappd: у Tankbusters одне пиво з цією назвою, номери — варіанти колаб-броварні. Проба на **815 рядках, уже злінкованих
пошуком** (`untappd_id_source = 'search'`; `beers.name` — текст крамниці/крана, назва bid — з Algolia `getObjects`,
`search-links.ts`) показала ту саму форму як систематичну: симетричне правило відкинуло б 21, з них ≈9 правильних, і
майже всі — номер **лише з боку Untappd** (`Výročni Vyřehtaná → … 20`, `Cucumber Gose → 10th Anniversary #6: Cucumber
Gose`, `Sybilla → 55 Lager Sybilla`, `Blackberry Gose → Sourberry #4`, `Life After Death Star 2025 → (Batch 7) 2025`,
`Idaho7 → Idaho 7`). Untappd дописує партію, ювілей, варіант колаборації — крамниці й паби їх не пишуть. Номер лише з
боку входу — навпаки, сильний сигнал іншого пива.

Чому правило однакове в матчері й у пошуку: якби матчер був строгим, а пошук поблажливим, кран `Owocowa Fantazja`
розлінковувався б матчером, пошук знаходив би `#1`, злиття ставило б `merged_at`, і `isRememberedMerge` тримав би
результат — той самий лінк ціною лукапу й злиття щоцикла. Якби строгими були обидва — ≈0,7 % правильних знахідок
пошуку ставали б вічними сиротами, кожна — кандидат на пін.

### Застосування

**Ядро.**

1. **Точна стадія матчера** (`matchPrepared`, після побудови `exacts` в обох гілках). Кандидати з `different`
   відкидаються. **Вхід без року:** `same` і `year-fallback` — одна група (кандидат з ABV у `ABV_TOLERANCE`, інакше
   найновіший `id`) — рівно сьогоднішня поведінка (`picks the latest vintage when ABV not provided`: кран без року
   бере найновіший вінтаж, а не старий рядок без року). **Вхід з роком:** група `same` (той самий рік), якщо
   непорожня, інакше група `year-fallback` (рядки без року); у групі — ABV, інакше найновіший `id`. **Лише коли обидві
   групи порожні** — група `number-fallback` (рядки з номером, якого вхід не несе), ABV, інакше найновіший `id`:
   `Juicy Trap` бере рядок `Juicy Trap`, а не новіший `Juicy Trap #20`; за відсутності ненумерованого — `#20`, як
   сьогодні. Результат лишається `source: 'exact'` (як сьогодні для тих самих рядків). Нюанс нинішнього
   коду зберігається: якщо перший кандидат групи `same` має
   ABV, що суперечить входу, а в `year-fallback` є кандидат з ABV у толерансі, — береться він (помилка ABV у лістингу
   проти вінтажу без року). Розбиття `yearMatch`/`noYear`/`wrongYear` і `extractYear` у матчері цим поглинаються;
   `wrongYear` — це `different`. **Свідома зміна поведінки:** сьогодні рядок того самого року з суперечливим ABV
   поступається рядку **іншого** року з ABV у толерансі (тест `year match + ABV mismatch + no noYear → wrongYear ABV
   hit wins`); тепер інший рік не бере ніколи, і лишається рядок того самого року — помилка ABV у лістингу дешевша
   за чужий вінтаж. Порожньо після фільтра → `null`, як нинішнє «Only wrong-year candidates exist»: точна стадія
   знайшла ряд, але не номер. Переходу у fuzzy немає — перевірено: її найкращий результат — той самий рядок чужого
   номера зі скором 1.0, гейт цифр його відкине, тож перехід непомітний у результаті, але на mis-split вході
   (порожній `breweryMatches`) витратив би бюджет повного fuzzy-пошуку.
2. **Fuzzy-стадія.** Якщо найкращий результат `different` → `null`; обидва запасні рівні приймаються. Глибше в результати не йдемо: нижчий скор
   після відкинутого точного збігу — вгадування, а сирота піде в пошук.

Через матчер правило отримують `/match` (розширення), `refresh-ontap` і `cleanup-polluted-ontap`. Цифри входу
читаються один раз на виклик; цифри кандидата — лише для `exacts` і найкращого fuzzy-результату, не для каталогу.

**Обв'язка.**

3. **`ensureBeerRow`.** Після аліаса (#614, без змін) — усі рядки пари `(normalized_brewery, normalized_name)`,
   картка — `input`, рядок — `candidate`; відкинути `different`, далі `same` > `year-fallback` > `number-fallback`,
   далі найменший `id` (детермінізм замість
   порядку `.get()`). Немає сумісного — `ensureOrphan`. `findBeerByNormalized` (єдиний виклик — `enrich.ts`)
   замінюється на `listBeersByNormalized`, що повертає всі рядки пари; вибір робить `ensureBeerRow`.
4. **`ensureOrphan` / `resolvableOrphan`.** Замість `numericTokensCompatible`:
   - `ensureOrphan` порівнює **рівних** — новий текст крана з текстом наявної сироти (обидва — вхідний текст, жоден не
     з Untappd), тож ролей немає: сумісні, лише якщо `digitIdentity` **в обидва боки** не `different` (номер з
     будь-якого боку — інша сирота). Окрема умова «не `number-fallback`» не потрібна: якщо `digitIdentity(a, b)` —
     `number-fallback`, то `digitIdentity(b, a)` — завжди `different` (той самий вираз `candidateOnly` стає
     непокритим числом входу);
   - `resolvableOrphan` (`upsertBeerByBid`): сирота — `input`, назва з синку чекінів/`/import` (назва Untappd) —
     `candidate`; сумісна **лише як `same` або `year-fallback`**. `number-fallback` тут не приймається: резолвлення
     записує bid у рядок сироти, і рядок зберігає текст крана (#618) — наступний інжест бачить `same` між краном і цим
     рядком, тож вгадування «`Juicy Trap 18°` — це `#19`, бо його чекін синкнувся першим» стало б ідентичністю
     назавжди (рев'ю асиметрії, 2026-09-16). Сьогоднішній `numericTokensCompatible` цей випадок теж відкидає.
   Семантика #617 («кран 2025 не прилипає до сироти 2024», «рік з одного боку сумісний») зберігається.
5. **`lookupBeer`.** На початку `matchAgainst(results)` — `results` фільтруються: кандидат Algolia з `different`
   щодо вхідної назви (`input` — назва крана/картки, `candidate` — `beer_name`) прибирається; **якщо серед решти є
   хоч один `same` чи `year-fallback`, кандидати `number-fallback` теж прибираються** — це фільтр, а не ранжування:
   наявні стадії (ABV, домінування, тай-брейк #409) вирішують серед того, що лишилось, як сьогодні. Без цього пошук
   міг би обрати `Juicy Trap #20` там, де матчер бере `Juicy Trap`, а злиття поставило б `merged_at`, і
   `isRememberedMerge` тримав би вибір пошуку проти матчера — рівно та розбіжність, проти якої «Асиметрія ролей»
   (рев'ю асиметрії, 2026-09-16). Усі пули (`strict`, `relaxed`, `native`, `brand`, `brandRemainder`),
   `identityHits`, `typoRescue`, czech-grade (#321) і флагман (#487) читають уже чистий список. Кандидати поза фільтром
   **лишаються** в `seenCandidates` (`not_found.candidates`) — це доказ для тріажу, а не матч.

### Порядок деплою: один PR, один деплой

План ділиться на ядро й обв'язку (див. «Стадії»), але **деплой один**. Ядро без гейту пошуку гірше за нинішній стан:
перший інжест розлінкує ≈20 кранів, вони стануть сиротами й підуть в inline-пошук, який сліпий до цифр
(`Dr.Hazy #7` → `#4`, доведено). Злиття в канонічний рядок `#4` поставить `merged_at`, а `isRememberedMerge` тримає
такий лінк і за `null` матчера — тимчасово хибний лінк стане запам'ятованим.

## Спростовані варіанти

- **Градус — ідентичність, коли назва без нього — голий бренд чи стиль** (перша погоджена версія). Прототип: розвело б
  92 живі лінки, більшість — хибно: `Pils 12° ↔ Pils`, `Harpagan Pils 12° ↔ Harpagan Pils`, `Weizen 12,5° ↔ Weizen`,
  `Blue Moon 12,5° ↔ Blue Moon`, пін `Guinness 9,9° ↔ Guinness Draught`. У каталозі Untappd градуса зазвичай немає,
  тож «градус з одного боку» — звичайний стан.
- **Градус проти градуса, різні → різні пива.** `Białe IPA 16° ↔ 14°`, `Kolaż 15,5° ↔ 14,5°`, `Mini Młot 8° ↔ 9°`,
  `Brutus 17° ↔ 16.5°` — те саме пиво, у каталозі інший екстракт. Єдиний справжній випадок — `Poutník 12° ↔ 11°`.
- **Цифра з одного боку — «невизначено», вирішує ABV.** У номерів однієї серії ABV часто однаковий (`Dr.Hazy #5`/`#7`),
  тож хибні лінки лишились би саме в найчастішому класі.
- **Не викидати цифри з `normalizeName`.** Змінює ключ 33 тис. рядків (міграція колонки), запити Algolia і fuzzy-пул;
  шумові цифри (`16°`, `4'8%`, рік з одного боку) почали б розводити те саме пиво.
- **Симетричне правило: число лише з одного боку — `different` в обидва боки** (перша погоджена версія, реалізована в
  ядрі). Спростоване після рев'ю ядра: відкидало ≈9 з 815 правильних знахідок пошуку і правильний fuzzy-лінк
  `Few More Beer`, бо Untappd дописує номери, яких крамниці не пишуть (див. «Асиметрія ролей»).
- **Латка в кожному місці окремо** — так правило вже опинилось лише в одному місці з п'яти (#617).

## Вимір правила (прототип `proto636c.ts`, режим `VER=1 CZ=1` + виняток для градуса проти м'якого числа; цифри — з модуля плану, `verify-module.ts`)

| набір | що це | результат |
|---|---|---|
| 1. Завідомо **різні**: пари злінкованих рядків з різними `untappd_id` і рівною нормалізованою парою — 1 658 (асиметрично — 3 316 напрямків; 47 з них `number-fallback`, усі — вхід без жодного номера: `Friendship → Friendship 2`, `Gloed → (Batch #7 - 2023)`, `Lagervulin → Lagervulin 16`) | Untappd сам каже, що це різні пива | симетрично: `different` 1 266; `year-fallback` 245 (розводить тай-брейк, коли в каталозі є обидва); `same` 147 — різниця у **словах** (`Leffe Blonde ↔ 0,0%`, `Double Harmonics ↔ Triple Harmonics`, `Blueprint (Motueka) ↔ (Simcoe)`), поза межами #636 |
| 2. Завідомо **однакові**: піни + `merged_at` — 55 | людина або доказ злиття | симетрично: `same` 52, `different` 3 — `Few More Beer 004/108` ×2 і `Stan Umysłu 2026 ↔ 2027` (ймовірно, саме хибне злиття). **Асиметрично:** `same` 52, `number-fallback` 2 (`Few More Beer 004/108` ×2), `different` 1 (`Stan Umysłu`) |
| 3. Живі точні автолінки кранів — 1 494 | що зміниться на першому інжесті | симетрично: `different` 27. **Асиметрично:** `same` 1 436, `year-fallback` 31, `number-fallback` 7, `different` 20 |
| 4. Живі **fuzzy**-автолінки кранів (`confidence < 1`, не пін, без `merged_at`) — 152 (додано після рев'ю ядра, `fuzzy-links.ts`) | набір 3 їх не міряв | симетрично: `different` 2. **Асиметрично:** `same` 143, `year-fallback` 7, `number-fallback` 1 (`Few More Beers 19° → Few More Beer 004/108`, правильний), `different` 1 (`Kronenbourg 1664 Blanc 12,5° → Kronenbourg`, conf 0.79; лінк хибний — лагер 4.2 % проти 5.0 на крані, сирота — поліпшення) |
| 5. Рядки, **злінковані пошуком** (`untappd_id_source = 'search'`), назва крамниці → назва bid в Algolia — 815 (`search-links.ts`) | що зробив би гейт пошуку з уже прийнятими знахідками | симетрично: `different` 21 (≈12 слушно, ≈9 правильних). **Асиметрично:** `same` 774, `year-fallback` 20, `number-fallback` 8 (6 правильних + `Icarus Brewery Pack → 128 Pack`, `Kellerbier → … 1000 Jahre Mering`), `different` 13 — чужі вінтажі/номери (`Stan Umysłu 2027 → 2025`, `Wednesday 2023 → (2017)`, `Abraxas 2025 → (2019)`, `6th → 5th Anniversary`, `Chapter 3 → 2`, `Blend No. 3 → No.1`, `Aged Bock 2018 → (2017)`, `Three Layers 2026 → 2025`, `Anniversary Blend 2026 → 2025`) і ціна (`Limited Exhibition 4: Rhapsody → Limited Exhibition - Rhapzody`, `3 Fonteinen 3 Fonteinen … No. 81` — бренд двічі в назві крамниці, `Inky 25 → (December 2025)`, `District 96 Brewery Pack`) |

27 лінків, що розійдуться: ≈20 справді хибних (див. «Доказ» + `IPA 11° → «10»`, `LAGER 10.5° → «21»` ×2,
`Ipa 10,5° → «15»`, `Paranormal Activity 2 → Paranormal Activity`, `Funky Monkey #2 → Funky Monkey`,
`Hoppy Grodzisz '26 → 23'`); невідомо, котрий номер на крані — сирота чесніша: `Owocowa Fantazja → #1`,
`The Book Of Hops → vol.4`, `10TH ANNIVERSARY 11° → no.5`, `Perfect Piece → (15th Anniversary …)`; **ціна** —
`Lagervulin 16 2.0 ↔ Lagervulin 2.0`, `Turbo%2 ↔ TURBO`, `Polska Desitka 10,5° ↔ 10,5/10`; спірний —
`Kronenbourg 1664 ↔ Kronenbourg`.

**`Few More Beer 004/108` лінкується правилом, пін не потрібен.** Спершу (симетричне правило) рішенням було курований
пін пари `(Tankbusters Brewery, Few More Beers 19°)`; асиметрія ролей робить його `number-fallback`, і fuzzy-гейт його
приймає. Реплей інжесту на асиметричному ядрі має це підтвердити (план доробки ядра).

27 → 20 лінків, що розійдуться асиметрично: переходять у `number-fallback` — `10TH ANNIVERSARY 11° → no.5`,
`Owocowa Fantazja → #1`, `Perfect Piece → (15th Anniversary …)`, `The Book Of Hops → vol.4` (кран без номера, рядок з
номером — найкраще доступне) і сміттєві `Ipa 10,5° → «15»`, `LAGER 10.5° → «21»` ×2 (назва крана зі стилю — окремий баг).

Поправки «версії з одного боку не розводять» і «м'які 8–14» разом коштують 17 із 1 285 розведень завідомо різних пар
(1,3 %) і прибирають 11 хибних розділень у наборах 2–3.

## Заявка → доказ

| заявка | доказ |
|---|---|
| Точна стадія лінкує кран на чужий номер | прод-БД: 14 з 1 494 живих автолінків, перелік вище |
| Fuzzy-стадія перелінкує той самий рядок, якщо лише точна відмовить | `probe636-fuzzy.ts` на прод-каталозі: 10 з 14 зі скором 1.000 |
| Гейт пошуку приймає кандидата чужого номера | живий реплей `probe636-lookup.ts`: `#7` і `#97` → `#4` |
| `ensureBeerRow` віддає картці рядок з іншими цифрами | `probe636-ensure.ts`: 468 пар, усі злінковані |
| Інжест переоцінює кожен кран, крім пінів, тож хибні лінки розійдуться без окремого проходу | код `refresh-ontap` (`getMatch` → `reviewed_by_user` пропускається, решта — `matchPrepared` щоцикла) |
| Злиті лінки переживуть `null` матчера | код `refresh-ontap`: гілка `isRememberedMerge` перед `ensureOrphan` |
| Серед живих fuzzy-лінків симетричне правило розводить 2, один з них правильний; асиметричне — 1, хибний | `fuzzy-links.ts`, `verify-asym.ts` на прод-БД; реплей інжесту main vs симетричне ядро (`replay-ingest-links.ts`) показав ті самі два |
| `Few More Beer` у Tankbusters — одне пиво | перевірка користувача на Untappd 2026-09-16 |
| Untappd систематично дописує номери, яких крамниці не пишуть; номер лише з боку входу — інше пиво | `search-links.ts` + `verify-asym.ts`: 815 знайдених пошуком рядків, 8 `number-fallback` (6 правильних), 13 `different` (9 слушних) |
| Правило розводить різне і не розводить однакове | прототип: 1 266 / 1 658 різних, 2 + 1 / 55 однакових (обидва — злиті лінки); модуль плану відтворює прототип попарно на всіх трьох наборах (`verify-module.ts`, 2 розбіжності — обидві `'25` після літери, виправлення) |
| Градус зберігається в назві крана до матчера | код `stripTrailingSpec` (`src/sources/ontap/identity.ts`), інваріант §5.2 |

**Заявки, яких спека свідомо НЕ робить:**
- про лінки кранів, яких немає в поточних знімках: їх ніхто не переоцінює; показ бере лише останній знімок, тож вони
  невидимі;
- про кількість уже реалізованих хибних `ensureBeerRow`: журнал `/enrich` не несе назви картки;
- що `Kronenbourg 1664` — те саме пиво, що `Kronenbourg`.

## Обмеження

- **Різниця у словах** (147 пар набору 1: `0,0%`, стиль, `Double`/`Triple`, хмелі в дужках) — не цифри; окрема
  проблема, лишається відкритою гілкою #636 або новим issue.
- **Прописні градуси** (`Desitka`, `dvanáctka`) цифрами не читаються; czech-grade (#321) у гейті пошуку їх і далі
  читає через `extractGrade`. `Kamenice Nad Lipou 10° ↔ Desitka` лишається `same`.
- **Приклеєні цифри** (`BA23.03 ↔ BA23.02`, `WFP10`) не розрізняються — інакше бренди на кшталт `Studio54` ламались би.
- **Діапазон м'яких чисел 8–14 ≠ `GRADE_MIN`/`GRADE_MAX` (7–20) з `czech-grade.ts`.** Той діапазон читає градус,
  коли вже відомо, що назва чеська; тут — будь-яке немарковане ціле, і 15–20 серед них переважно номери
  (`Juicy Trap 19`). 8–14 — виміряний діапазон, не перевикористання константи.
- **Голе ціле 8–14 без маркера** в не-чеській назві (`Session 12`) — м'яке, тож не розводить з `Session`.
- **Сезон `2024/25`** читається як рік 2024 плюс жорстке число 25, тож `BA 2024/25 ↔ (2024)` — `different`. У каталозі 4
  такі назви, жодної пари-близнюка не розводить (рев'ю ядра).
- **Версія з одного боку проти числа з іншого** (`Mango 2.0` → `Mango 2`) — `different` (версія входу непокрита при
  числі лише в кандидата); 0 пар у каталозі.
- **Обʼєм з пробілом** (`0,33 L`, `500 ml`) і **ABV без `%` і без `abv`** (`Pale Ale 4.5`) читаються як числа. На живих
  кранах — 0 (обʼєм) і 1 справжня назва (`Edward 11.1`); експозиція — картки `/match` крамниць, чий адаптер не зрізає
  обʼєм (невідома: журнал не несе назв карток).
- **Кран без номера при нумерованих рядках** бере `number-fallback`: єдиний нумерований — його (`Owocowa Fantazja →
  #1`), кілька — ABV, інакше найновіший `id` (як для вінтажів). Це вгадування, а не ідентичність; 47 напрямків набору 1.
- **Точна стадія з `number-fallback` не пускає у fuzzy.** `Mango Lassi` при рядках `Mango Lassi #2` і `Mango Lasi`
  (друкарська помилка в каталозі) бере `#2` точною стадією, хоча fuzzy знайшла б `Mango Lasi` (0.92). Так само на
  main; прийнято як ціна рівня (рев'ю асиметрії).
- **Рік входу не входить у захист `inputOwnUnmatched`.** `Abraxas 2025` → `Abraxas #3` — `number-fallback`
  (рік з одного боку і номер з одного боку — два слабкі сигнали, жоден не `different`). Закріплено тестом як
  задокументовану межу.
- **Назва крана зі стилю лінкується на рядок-число.** `LAGER 10.5°` → рядок `21`, `Imperial Stout` → `#100` —
  `number-fallback`, як і на main. Корінь — назви зі стилю (нормалізована назва порожня), окремий issue; фільтр «у
  кандидата після цифр мають лишатися літери» розглянуто й відкладено до того issue.
- **Бренд, повторений у назві крамниці** (`Brouwerij 3 Fonteinen 3 Fonteinen …`) дає зайве число входу → `different`;
  в Untappd бренд один раз. 1 з 815.
- **Односторонній рік** не розводить: коли в каталозі лише базовий рядок, кран «2026» ляже на нього (як сьогодні).

## Тести

TDD; кожен тест доводиться мутацією («видали рядок — тест падає»).

- `src/domain/digit-identity.test.ts` — таблиця реальних пар із проби, по рядку на правило: ABV `4'8%`; градус м'який
  (`Pils 12° ↔ Pils`, `Białe IPA 16° ↔ 14°`); градус покриває число (`Otakar 11° ↔ Otakar 11`); градус проти м'якого
  (`KONRAD 12° ↔ Konrad Svetlé Výčepní 10`); `'26 = 2026`; рік у дужках, діапазон `2021/2022 ≠ 2022/2023`;
  `vol.01`/`No.38`; `002 = 2`; `10th`; версії `2.0` з одного боку / `9.0 ≠ 5.0`; м'які 8–14 (`Svijanský Máz 11 ↔
  Svijanský Máz`); жорсткі (`Paranormal Activity 2`, `Kronenbourg 1664`, `Funky Monkey #2`); приклеєні (`WFP10`) не
  читаються; задокументована ціна (`Polska Desitka 10,5° ↔ 10,5/10` → `different`).
- `matcher` — дві пари рядків одного ряду з форм прод-каталогу (`Piwne Podziemie / Juicy Trap #19`/`#20`,
  `Przetwórnia Chmielu / Przetwór #3`), не синтетичні (урок #632: синтетичний каталог змінює fuzzy-пул): точна стадія
  бере свій номер; номера немає → fuzzy не перелінковує той самий ряд (`null`); split-invariant гілка — те саме; для входу
  з роком `same` переважає `year-fallback`, для входу без року — найновіший вінтаж, як сьогодні; нюанс ABV-помилки у `same` зберігається; наявні тести вінтажів проходять без змін, крім одного свідомо зміненого (`wrongYear ABV hit wins`, див. «Застосування»).
- `enrich` route — `ensureBeerRow`: картка `Trappistes Rochefort 10` при рядках `6` і `10` отримує `10`; при рядку лише
  `6` — нову сироту.
- `beers` — наявні тести #617 (`ensureOrphan`, `resolvableOrphan`) проходять на новій функції.
- `untappd-lookup` — фікстура пулу Algolia з `Dr. Hazy #4` на вході `Dr.Hazy #7` → `not_found`, кандидат у
  `candidates`; вхід `Dr.Hazy #4` → matched.

## Спостереження після деплою (чекпойнт)

Перед деплоєм — знімок автолінків (`ontap_ref`, `brewery_ref`, `untappd_beer_id`) у `~/warsaw-beer-probes/636/`.
Після першого інжесту (00:0x або 12:0x UTC):

1. Змінились саме передбачені лінки — набори 3 і 4 (див. рядок «Асиметрично»),
   з поправкою на крани, що зникли зі знімків; решта не рухалась. Асиметрично — набір 3 (20) і набір 4 (1, `Kronenbourg
   1664 Blanc`); `Few More Beers 19°` лишається злінкованим без піна. Порівнювати з реплеєм `replay-ingest-links.ts`, знятим
   на main безпосередньо перед деплоєм (каталог дрейфує між інжестами — контроль на незмінному main 2026-09-16 уже дав
   9 змін).
2. Нові сироти з розведених кранів не отримали bid чужого номера: `digitIdentity` назви сироти проти назви bid у
   Algolia (`getObjects`).
3. Жодного `merged duplicate orphan into canonical` з канонічним рядком, чиї цифри `different` від сироти.

## Документи

- `spec.md`: розділ матчера (точна стадія, split-invariant «проходять ту саму ABV/vintage-дизамбіґуацію», fuzzy-гейт);
  `lookupBeer` — фільтр цифр до пулів; `/enrich` — вибір рядка пари; §5.2 — новий інваріант «одна функція ідентичності
  цифр назви для всіх місць; різні числа з обох боків — різні пива», уточнення інваріанту «°Plato-градус — частина
  назви» (градус м'який: покриває число, сам не розводить).
- `src/domain/name-identity.test.ts:104` — застарілий коментар «`extractYear` розбиває вхід матчера» (матчер його більше
  не викликає).
- Розширення не змінюється — `extension/**`, `docs/extension-install-uk.md`, `CHANGELOG.md` не зачіпаються.

## Стадії

Спека одна. Плани — два (правило `CLAUDE.md` про великі зміни):

1. **Ядро:** `digit-identity.ts` + точна й fuzzy-стадії матчера; наскрізне рев'ю. **Доробка ядра** (асиметрія ролей і
   рівень `number-fallback`) — окремий план `2026-09-16-636-digit-identity-core-asymmetric.md`, до плану обв'язки.
2. **Обв'язка** (пишеться після рев'ю ядра): `ensureBeerRow`, `ensureOrphan`/`resolvableOrphan`, `lookupBeer`,
   `spec.md`.

Обидва плани — одна гілка, один PR, один деплой (див. «Порядок деплою»).
