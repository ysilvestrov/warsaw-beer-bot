# Popup Token-State Hierarchy Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** In the popup's no-token state, lead with the two controls that can actually help (`Get a token`, the setup guide), disable the one that cannot succeed (`Sync my check-ins`), and stop hiding the setup guide from users who already have a token.

**Architecture:** One pure function maps `hasToken` to a view model, and one applier writes that view model into a fixed DOM — the same reducer-plus-effects shape `extension/src/popup/clear-cache.ts` already uses. `popup.ts` calls the pair once and gates its sync wiring on the same boolean. No new state, no storage, no network.

**Tech Stack:** TypeScript, Chrome MV3 extension, Vite, Vitest + jsdom.

**Spec:** `docs/superpowers/specs/2026-09/2026-09-01-519-522-token-state-design.md`

## Global Constraints

- Exact caption string, verbatim, no variants: `Add a token to sync your check-ins.`
- The setup-guide link is visible in **both** token states. `guideLinkVisible` is deleted, not inverted.
- `#authBlock` is inserted **immediately after** `<header class="head">` — never `card.prepend`, which would put it above the logo and title.
- No `aria-live` attribute may appear in `popup.html`. Live regions are armed after init by `armLiveRegions` (#524); an existing test enforces this.
- Do not hide, collapse or move `Refresh this page` or `Clear all cache` in the no-token state. The spec refutes that part of #519 explicitly.
- Do not change the wording of `authNoteText`.
- No new CSS colour tokens. Both button tiers (`btn-primary`, `btn-secondary`) already exist.
- Per task, the full gate: `cd extension && npm test && npm run typecheck`, and `npm test && npm run typecheck` at the repo root. Not a scoped subset — a scoped gate let a regression survive four clean reviews on #527.
- Every new test must be mutation-proven: delete or invert the line under test, watch that test fail, restore. State the proof in the commit message or the report.
- Commit messages: conventional prefix plus the issue, e.g. `feat(#519): …`, `fix(#522): …`.

---

## File Structure

| File | Responsibility |
| --- | --- |
| `extension/src/popup/token-state.ts` (create) | `tokenStateView(hasToken)` — the whole decision table; `applyTokenState(nodes, view)` — the only code that writes it into the DOM |
| `extension/src/popup/token-state.test.ts` (create) | Table test for the view model; jsdom test for the applier, both directions |
| `extension/src/popup/popup.html` (modify) | `<section id="authBlock">` wrapper; `#guideLink` relocated into `<footer>` before `#clearAll` |
| `extension/src/popup/popup.css` (modify) | `.auth` takes over the hairline and top padding that `.note` carried |
| `extension/src/popup/popup.ts` (modify) | Delete `guideLinkVisible`; call the view model; gate the sync wiring and `poll()` on `hasToken` |
| `extension/src/popup/popup.test.ts` (modify) | Drop the `guideLinkVisible` suite; add markup guards; add the init integration test |
| `spec.md`, `docs/extension-install-uk.md`, `extension/CHANGELOG.md` (modify) | The two paragraphs this change makes false, plus the release note |

---

### Task 1: The view model and its applier

**Files:**
- Create: `extension/src/popup/token-state.ts`
- Test: `extension/src/popup/token-state.test.ts`

**Interfaces:**
- Consumes: nothing from earlier tasks.
- Produces (Task 3 imports these):
  - `interface TokenStateView { authVisible: boolean; syncTier: 'btn-primary' | 'btn-secondary'; authTier: 'btn-primary' | 'btn-secondary'; syncEnabled: boolean; syncCaption: string; guideHome: 'auth' | 'foot' }`
  - `interface TokenStateNodes { card: HTMLElement; header: HTMLElement; authBlock: HTMLElement; syncBtn: HTMLButtonElement; syncStatus: HTMLElement; getTokenBtn: HTMLButtonElement; guideLink: HTMLAnchorElement; foot: HTMLElement }`
  - `function tokenStateView(hasToken: boolean): TokenStateView`
  - `function applyTokenState(nodes: TokenStateNodes, view: TokenStateView): void`

- [ ] **Step 1: Write the failing tests**

Create `extension/src/popup/token-state.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { applyTokenState, tokenStateView, type TokenStateNodes } from './token-state';

function fixture(): TokenStateNodes & { root: HTMLElement } {
  document.body.innerHTML = `
    <main class="card">
      <header class="head"><h1>Warsaw Beer Overlay</h1></header>
      <button id="syncCheckins" class="btn btn-primary" type="button"><span>Sync my check-ins</span></button>
      <p id="syncStatus" class="status"></p>
      <button id="refresh" class="btn btn-secondary" type="button"><span>Refresh this page</span></button>
      <p id="refreshStatus" class="status"></p>
      <details class="shops"><summary>Supported shops</summary></details>
      <section id="authBlock" class="auth" style="display:none">
        <p id="authNote" class="note"></p>
        <button id="getToken" class="btn btn-secondary" type="button"><span>Get a token</span></button>
      </section>
      <footer class="foot">
        <a id="guideLink" class="guide" style="display:none">Read the setup guide →</a>
        <button id="clearAll" class="btn btn-ghost" type="button"><span>Clear all cache</span></button>
        <p id="clearStatus" class="status status-foot"></p>
      </footer>
    </main>`;
  const q = <T extends HTMLElement>(sel: string) => document.querySelector(sel) as T;
  return {
    root: q('.card'),
    card: q('.card'),
    header: q('header.head'),
    authBlock: q('#authBlock'),
    syncBtn: q<HTMLButtonElement>('#syncCheckins'),
    syncStatus: q('#syncStatus'),
    getTokenBtn: q<HTMLButtonElement>('#getToken'),
    guideLink: q<HTMLAnchorElement>('#guideLink'),
    foot: q('footer.foot'),
  };
}

describe('tokenStateView', () => {
  it('without a token, the auth block leads and sync cannot be used', () => {
    expect(tokenStateView(false)).toEqual({
      authVisible: true,
      syncTier: 'btn-secondary',
      authTier: 'btn-primary',
      syncEnabled: false,
      syncCaption: 'Add a token to sync your check-ins.',
      guideHome: 'auth',
    });
  });

  it('with a token, sync is the primary action and the guide moves to the footer', () => {
    expect(tokenStateView(true)).toEqual({
      authVisible: false,
      syncTier: 'btn-primary',
      authTier: 'btn-secondary',
      syncEnabled: true,
      syncCaption: '',
      guideHome: 'foot',
    });
  });
});

describe('applyTokenState — no token', () => {
  it('puts the auth block directly after the header, never above it', () => {
    const n = fixture();
    applyTokenState(n, tokenStateView(false));
    expect(n.card.firstElementChild).toBe(n.header);
    expect(n.header.nextElementSibling).toBe(n.authBlock);
    expect(n.authBlock.style.display).toBe('');
  });

  it('moves the guide link next to Get a token and shows it', () => {
    const n = fixture();
    applyTokenState(n, tokenStateView(false));
    expect(n.authBlock.contains(n.guideLink)).toBe(true);
    expect(n.guideLink.style.display).toBe('');
  });

  it('disables sync, demotes it, and says what is missing', () => {
    const n = fixture();
    applyTokenState(n, tokenStateView(false));
    expect(n.syncBtn.disabled).toBe(true);
    expect(n.syncBtn.classList.contains('btn-secondary')).toBe(true);
    expect(n.syncBtn.classList.contains('btn-primary')).toBe(false);
    expect(n.syncStatus.textContent).toBe('Add a token to sync your check-ins.');
  });

  it('promotes Get a token to the primary tier', () => {
    const n = fixture();
    applyTokenState(n, tokenStateView(false));
    expect(n.getTokenBtn.classList.contains('btn-primary')).toBe(true);
    expect(n.getTokenBtn.classList.contains('btn-secondary')).toBe(false);
  });
});

describe('applyTokenState — token present', () => {
  it('hides the auth block and keeps the guide link visible in the footer', () => {
    const n = fixture();
    applyTokenState(n, tokenStateView(true));
    expect(n.authBlock.style.display).toBe('none');
    expect(n.foot.contains(n.guideLink)).toBe(true);
    expect(n.guideLink.style.display).toBe('');
  });

  it('leaves sync primary, enabled and uncaptioned', () => {
    const n = fixture();
    applyTokenState(n, tokenStateView(true));
    expect(n.syncBtn.disabled).toBe(false);
    expect(n.syncBtn.classList.contains('btn-primary')).toBe(true);
    expect(n.syncStatus.textContent).toBe('');
  });
});

describe('applyTokenState is reversible', () => {
  it('leaves no residue of the no-token state when re-applied with a token', () => {
    const n = fixture();
    applyTokenState(n, tokenStateView(false));
    applyTokenState(n, tokenStateView(true));
    expect(n.authBlock.style.display).toBe('none');
    expect(n.foot.contains(n.guideLink)).toBe(true);
    expect(n.guideLink.style.display).toBe('');
    expect(n.syncBtn.disabled).toBe(false);
    expect(n.syncBtn.classList.contains('btn-primary')).toBe(true);
    expect(n.syncBtn.classList.contains('btn-secondary')).toBe(false);
    expect(n.syncStatus.textContent).toBe('');
    expect(n.getTokenBtn.classList.contains('btn-secondary')).toBe(true);
  });

  it('leaves no residue of the token state when re-applied without one', () => {
    const n = fixture();
    applyTokenState(n, tokenStateView(true));
    applyTokenState(n, tokenStateView(false));
    expect(n.header.nextElementSibling).toBe(n.authBlock);
    expect(n.authBlock.contains(n.guideLink)).toBe(true);
    expect(n.syncBtn.disabled).toBe(true);
    expect(n.syncStatus.textContent).toBe('Add a token to sync your check-ins.');
  });
});
```

- [ ] **Step 2: Run the tests and watch them fail**

Run: `cd extension && npx vitest run src/popup/token-state.test.ts`
Expected: FAIL — `Failed to resolve import "./token-state"`.

- [ ] **Step 3: Write the implementation**

Create `extension/src/popup/token-state.ts`:

```ts
// #519/#522: the popup's hierarchy is a function of one fact — whether a token
// exists. Without one, `Sync my check-ins` cannot succeed (the background finds
// no token, stores outcome:'error' and the popup prints a connection error),
// and it would first raise a host-permission prompt for untappd.com on the way
// there. So in that state it is demoted, disabled and captioned, while the two
// controls that CAN help — `Get a token` and the setup guide — lead the card.

export interface TokenStateView {
  /** The auth block is shown, directly after the header. Only ever true without a token. */
  authVisible: boolean;
  syncTier: 'btn-primary' | 'btn-secondary';
  authTier: 'btn-primary' | 'btn-secondary';
  syncEnabled: boolean;
  /** Empty when the sync button is enabled — the caption belongs to whoever last clicked. */
  syncCaption: string;
  /** Where the setup-guide link lives. It is visible in both states (#522). */
  guideHome: 'auth' | 'foot';
}

export interface TokenStateNodes {
  card: HTMLElement;
  header: HTMLElement;
  authBlock: HTMLElement;
  syncBtn: HTMLButtonElement;
  syncStatus: HTMLElement;
  getTokenBtn: HTMLButtonElement;
  guideLink: HTMLAnchorElement;
  foot: HTMLElement;
}

export function tokenStateView(hasToken: boolean): TokenStateView {
  return hasToken
    ? {
        authVisible: false,
        syncTier: 'btn-primary',
        authTier: 'btn-secondary',
        syncEnabled: true,
        syncCaption: '',
        guideHome: 'foot',
      }
    : {
        authVisible: true,
        syncTier: 'btn-secondary',
        authTier: 'btn-primary',
        syncEnabled: false,
        syncCaption: 'Add a token to sync your check-ins.',
        guideHome: 'auth',
      };
}

function setTier(btn: HTMLElement, tier: 'btn-primary' | 'btn-secondary'): void {
  btn.classList.remove('btn-primary', 'btn-secondary');
  btn.classList.add(tier);
}

/** The only code that writes a TokenStateView into the DOM. It never reads state back out. */
export function applyTokenState(nodes: TokenStateNodes, view: TokenStateView): void {
  // The guide link moves before the block does, so it travels with the block.
  const guideHost = view.guideHome === 'auth' ? nodes.authBlock : nodes.foot;
  if (view.guideHome === 'foot') guideHost.prepend(nodes.guideLink);
  else guideHost.append(nodes.guideLink);
  nodes.guideLink.style.display = '';

  if (view.authVisible) {
    nodes.header.after(nodes.authBlock);
    nodes.authBlock.style.display = '';
  } else {
    nodes.authBlock.style.display = 'none';
  }

  setTier(nodes.syncBtn, view.syncTier);
  setTier(nodes.getTokenBtn, view.authTier);
  nodes.syncBtn.disabled = !view.syncEnabled;
  nodes.syncStatus.textContent = view.syncCaption;
}
```

- [ ] **Step 4: Run the tests and watch them pass**

Run: `cd extension && npx vitest run src/popup/token-state.test.ts`
Expected: PASS, 8 tests.

- [ ] **Step 5: Mutation-prove each new test**

For each of these, make the change, run the file, confirm the named test fails, then restore:

| Mutation | Test that must go red |
| --- | --- |
| `nodes.header.after(...)` → `nodes.card.prepend(...)` | "puts the auth block directly after the header, never above it" |
| delete `nodes.guideLink.style.display = '';` | "moves the guide link next to Get a token and shows it" and the footer one |
| delete `nodes.syncBtn.disabled = !view.syncEnabled;` | "disables sync, demotes it, and says what is missing" |
| in `setTier`, drop the `classList.remove` line | both reversibility tests |
| `syncCaption: 'Add a token to sync your check-ins.'` → `''` | "disables sync, demotes it, and says what is missing" |

- [ ] **Step 6: Run the full gate**

Run: `cd extension && npm test && npm run typecheck`
Then: `cd .. && npm test && npm run typecheck`
Expected: all green.

- [ ] **Step 7: Commit**

```bash
git add extension/src/popup/token-state.ts extension/src/popup/token-state.test.ts
git commit -m "feat(#519,#522): the popup's hierarchy as a function of hasToken"
```

---

### Task 2: Markup and stylesheet

**Files:**
- Modify: `extension/src/popup/popup.html:54-72`
- Modify: `extension/src/popup/popup.css:240-259`
- Test: `extension/src/popup/popup.test.ts` (append to the existing `describe('popup markup')`)

**Interfaces:**
- Consumes: nothing at runtime; Task 1's applier expects the ids and structure this task creates.
- Produces: `#authBlock` (a `<section class="auth">` wrapping `#authNote` and `#getToken`); `#guideLink` living inside `<footer class="foot">` before `#clearAll`; both starting hidden.

- [ ] **Step 1: Write the failing markup guards**

Append to the existing `describe('popup markup', …)` block at the end of `extension/src/popup/popup.test.ts` (the `html` const is already defined there):

```ts
  it('wraps the note and Get a token in one movable block (#519)', () => {
    expect(html).toContain('id="authBlock"');
    const block = html.match(/<section id="authBlock"[\s\S]*?<\/section>/)?.[0] ?? '';
    expect(block).toContain('id="authNote"');
    expect(block).toContain('id="getToken"');
  });

  it('parks the guide link in the footer ahead of the destructive action (#522)', () => {
    const foot = html.match(/<footer class="foot">[\s\S]*?<\/footer>/)?.[0] ?? '';
    expect(foot).toContain('id="guideLink"');
    expect(foot.indexOf('id="guideLink"')).toBeLessThan(foot.indexOf('id="clearAll"'));
  });

  it('ships the auth block and the guide link hidden, so init places them before they are seen', () => {
    const block = html.match(/<section id="authBlock"[^>]*>/)?.[0] ?? '';
    expect(block).toContain('display:none');
    const link = html.match(/<a id="guideLink"[^>]*>/)?.[0] ?? '';
    expect(link).toContain('display:none');
  });
```

- [ ] **Step 2: Run them and watch them fail**

Run: `cd extension && npx vitest run src/popup/popup.test.ts -t 'popup markup'`
Expected: FAIL — no `id="authBlock"` in the markup.

- [ ] **Step 3: Change the markup**

In `extension/src/popup/popup.html`, replace the current block (the `#authNote` paragraph, the `#getToken` button and the `#guideLink` anchor, lines 54-61) with:

```html
      <section id="authBlock" class="auth" style="display:none">
        <p id="authNote" class="note"></p>
        <button id="getToken" class="btn btn-secondary" type="button">
          <svg class="ico" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
            <path d="M21 2l-2 2m-7.61 7.61a5.5 5.5 0 1 1-7.778 7.778 5.5 5.5 0 0 1 7.777-7.777zm0 0L15.5 7.5m0 0l3 3L22 7l-3-3" />
          </svg>
          <span>Get a token</span>
        </button>
      </section>
```

and put the anchor at the top of the footer, before `#clearAll`:

```html
      <footer class="foot">
        <a id="guideLink" class="guide" target="_blank" rel="noopener" style="display:none">Read the setup guide →</a>
        <button id="clearAll" class="btn btn-ghost" type="button">
```

Leave the rest of the footer (the SVG, the label span, `#clearStatus`) exactly as it is.

- [ ] **Step 4: Move the hairline from the note to the wrapper**

In `extension/src/popup/popup.css`, replace the `.note` rule:

```css
.note {
  margin: 0 0 8px;
  padding-top: 12px;
  border-top: 1px solid var(--hairline);
  color: var(--fg-muted);
  font-size: 12px;
}
```

with:

```css
/* #519: the block moves as a unit to the top of the card, so the rule that
   fences it off belongs to the wrapper, not to the note's first line. */
.auth {
  padding-top: 12px;
  border-top: 1px solid var(--hairline);
  margin-bottom: 8px;
}

.note {
  margin: 0 0 8px;
  color: var(--fg-muted);
  font-size: 12px;
}
```

- [ ] **Step 5: Run the tests and watch them pass**

Run: `cd extension && npx vitest run src/popup/popup.test.ts`
Expected: PASS. The pre-existing markup guards (no `aria-live`, no `<h2>`, no `aria-describedby`, `#refreshStatus` and `#clearStatus` present) must still pass — if any of them fails, the markup edit went wrong; fix the markup, never the guard.

- [ ] **Step 6: Mutation-prove the new guards**

| Mutation | Test that must go red |
| --- | --- |
| move `#getToken` back outside `<section id="authBlock">` | "wraps the note and Get a token in one movable block" |
| move `<a id="guideLink">` after `#clearAll` in the footer | "parks the guide link in the footer ahead of the destructive action" |
| drop `style="display:none"` from the `<section>` | "ships the auth block and the guide link hidden…" |

- [ ] **Step 7: Run the full gate**

Run: `cd extension && npm test && npm run typecheck`
Then: `cd .. && npm test && npm run typecheck`
Expected: green. `popup.ts` still finds every id it queries, so nothing else moves yet.

- [ ] **Step 8: Commit**

```bash
git add extension/src/popup/popup.html extension/src/popup/popup.css extension/src/popup/popup.test.ts
git commit -m "feat(#519,#522): make the auth block one movable section and park the guide in the footer"
```

---

### Task 3: Wire it into the popup

**Files:**
- Modify: `extension/src/popup/popup.ts:86-88` (delete `guideLinkVisible`), `:153-171` (the auth block), `:184-236` (the sync wiring)
- Modify: `extension/src/popup/popup.test.ts:4` (import line), `:131-139` (delete the `guideLinkVisible` suite)
- Test: `extension/src/popup/popup.test.ts` (new `describe('initPopup')`)

**Interfaces:**
- Consumes from Task 1: `tokenStateView(hasToken: boolean): TokenStateView`, `applyTokenState(nodes: TokenStateNodes, view: TokenStateView): void`, and the `TokenStateNodes` shape `{ card, header, authBlock, syncBtn, syncStatus, getTokenBtn, guideLink, foot }`.
- Consumes from Task 2: the ids `#authBlock`, `#guideLink` (in the footer), and the selectors `.card`, `header.head`, `footer.foot`.
- Produces: nothing further; this is the last code task.

- [ ] **Step 1: Write the failing integration test**

Add to `extension/src/popup/popup.test.ts`. It drives the real `initPopup` by importing the module after the DOM and the `chrome` stubs are in place — the module self-starts when `#refresh` exists (`popup.ts:241`). The shared `chrome` stub from `extension/tests/setup.ts` provides `storage.local` and `runtime.sendMessage`; this test adds `tabs`, `permissions` and `runtime.openOptionsPage` on top of it.

```ts
describe('initPopup', () => {
  const html = readFileSync(resolve(__dirname, 'popup.html'), 'utf8');

  async function bootPopup(token: string) {
    // Only the body content: assigning a fragment to documentElement would leave
    // jsdom without a <body> for popup.ts to query.
    document.body.innerHTML = html.split('<body>')[1].split('</body>')[0];
    const sendMessage = vi.fn();
    const chromeStub = globalThis.chrome as unknown as Record<string, unknown>;
    await (chromeStub.storage as { local: { set(o: object): Promise<void> } }).local.set({ token });
    chromeStub.tabs = { query: vi.fn(async () => [{ id: 7, url: 'https://beerfreak.org/p/1' }]) };
    chromeStub.permissions = { request: vi.fn(async () => true) };
    (chromeStub.runtime as Record<string, unknown>).sendMessage = sendMessage;
    (chromeStub.runtime as Record<string, unknown>).openOptionsPage = vi.fn();
    vi.resetModules();
    await import('./popup');
    await new Promise((r) => setTimeout(r, 0));
    return { sendMessage };
  }

  it('without a token: sync is disabled, captioned, and never polled (#519)', async () => {
    const { sendMessage } = await bootPopup('');
    const syncBtn = document.getElementById('syncCheckins') as HTMLButtonElement;
    expect(syncBtn.disabled).toBe(true);
    expect(document.getElementById('syncStatus')?.textContent).toBe('Add a token to sync your check-ins.');
    expect(sendMessage.mock.calls.some(([m]) => m?.type === 'checkin-sync:status')).toBe(false);
  });

  it('without a token: the auth block leads and the guide sits inside it (#519, #522)', async () => {
    await bootPopup('');
    const header = document.querySelector('header.head');
    const authBlock = document.getElementById('authBlock') as HTMLElement;
    expect(header?.nextElementSibling).toBe(authBlock);
    expect(authBlock.contains(document.getElementById('guideLink'))).toBe(true);
    expect((document.getElementById('guideLink') as HTMLElement).style.display).toBe('');
  });

  it('with a token: the guide link stays visible in the footer (#522)', async () => {
    await bootPopup('tok');
    const guide = document.getElementById('guideLink') as HTMLAnchorElement;
    expect(document.querySelector('footer.foot')?.contains(guide)).toBe(true);
    expect(guide.style.display).toBe('');
    expect(guide.href).toBe(SETUP_GUIDE_URL);
    expect((document.getElementById('authBlock') as HTMLElement).style.display).toBe('none');
  });

  it('with a token: sync is primary and its status is polled', async () => {
    const { sendMessage } = await bootPopup('tok');
    const syncBtn = document.getElementById('syncCheckins') as HTMLButtonElement;
    expect(syncBtn.classList.contains('btn-primary')).toBe(true);
    expect(sendMessage.mock.calls.some(([m]) => m?.type === 'checkin-sync:status')).toBe(true);
  });
});
```

- [ ] **Step 2: Run it and watch it fail**

Run: `cd extension && npx vitest run src/popup/popup.test.ts -t 'initPopup'`
Expected: FAIL — the caption is empty and `checkin-sync:status` is sent even without a token.

- [ ] **Step 3: Delete `guideLinkVisible` and its suite**

In `extension/src/popup/popup.ts`, delete lines 86-88 entirely:

```ts
/** The setup-guide link is shown in the same no-token state as the auth note. */
export function guideLinkVisible(hasToken: boolean): boolean {
  return !hasToken;
}
```

In `extension/src/popup/popup.test.ts`, remove `guideLinkVisible` from the import on line 4, and delete this suite outright:

```ts
describe('guideLinkVisible', () => {
  it('shows the guide link only when there is no token', () => {
    expect(guideLinkVisible(false)).toBe(true);
    expect(guideLinkVisible(true)).toBe(false);
  });
  it('links to the hosted setup guide', () => {
    expect(SETUP_GUIDE_URL).toContain('/install/');
  });
});
```

Keep the `SETUP_GUIDE_URL` import — the new `initPopup` test asserts against it.

- [ ] **Step 4: Replace the auth-block wiring**

In `extension/src/popup/popup.ts`, add the import beside the existing popup-local imports:

```ts
import { applyTokenState, tokenStateView } from './token-state';
```

and replace the whole block currently at lines 153-171 (from `const authNote = el<HTMLElement>('authNote');` down to the closing brace of the `if (authNote && getTokenBtn && guideLink)` statement) with:

```ts
  const authNote = el<HTMLElement>('authNote');
  const authBlock = el<HTMLElement>('authBlock');
  const getTokenBtn = el<HTMLButtonElement>('getToken');
  const guideLink = el<HTMLAnchorElement>('guideLink');
  const syncBtn = el<HTMLButtonElement>('syncCheckins');
  const syncStatus = el<HTMLElement>('syncStatus');
  const card = document.querySelector<HTMLElement>('.card');
  const header = document.querySelector<HTMLElement>('header.head');
  const foot = document.querySelector<HTMLElement>('footer.foot');

  const { token } = await getSettings();
  const hasToken = Boolean(token);

  if (authNote && authBlock && getTokenBtn && guideLink && syncBtn && syncStatus && card && header && foot) {
    authNote.textContent = authNoteText(hasToken) ?? '';
    guideLink.href = SETUP_GUIDE_URL;
    getTokenBtn.addEventListener('click', () => chrome.runtime.openOptionsPage());
    applyTokenState(
      { card, header, authBlock, syncBtn, syncStatus, getTokenBtn, guideLink, foot },
      tokenStateView(hasToken),
    );
  }
```

Note what moved: `guideLink.href` and the `Get a token` handler are now set in both states (the link is visible in both, and the button is inside a block that is merely hidden), while placement, tier, `disabled` and the caption belong to `applyTokenState`.

- [ ] **Step 5: Gate the sync wiring on the token**

In `extension/src/popup/popup.ts`, the sync block currently opens with:

```ts
  const syncBtn = el<HTMLButtonElement>('syncCheckins');
  const syncStatus = el<HTMLElement>('syncStatus');
  if (syncBtn && syncStatus) {
```

Delete those two `const` lines — Step 4 already declared both — and change the condition to:

```ts
  if (hasToken && syncBtn && syncStatus) {
```

Leave the body of that block, including the trailing `poll();`, untouched: it now runs only when a token exists, so nothing overwrites the caption `applyTokenState` wrote.

- [ ] **Step 6: Run the tests and watch them pass**

Run: `cd extension && npx vitest run src/popup/popup.test.ts`
Expected: PASS, including the four `initPopup` tests and every pre-existing suite.

- [ ] **Step 7: Mutation-prove the new tests**

| Mutation | Test that must go red |
| --- | --- |
| `if (hasToken && syncBtn && syncStatus)` → `if (syncBtn && syncStatus)` | "without a token: sync is disabled, captioned, and never polled" |
| delete the `applyTokenState(...)` call | "without a token: the auth block leads and the guide sits inside it" |
| `guideLink.href = SETUP_GUIDE_URL;` moved back inside a no-token branch | "with a token: the guide link stays visible in the footer" |

- [ ] **Step 8: Run the full gate**

Run: `cd extension && npm test && npm run typecheck`
Then: `cd .. && npm test && npm run typecheck`
Expected: all green. Confirm the extension suite count went up rather than down — a deleted suite plus new ones must net positive.

- [ ] **Step 9: Build the extension to prove the popup still bundles**

Run: `cd extension && npm run build`
Expected: a clean Vite build (this catches an id or selector that only exists in the test fixture).

- [ ] **Step 10: Commit**

```bash
git add extension/src/popup/popup.ts extension/src/popup/popup.test.ts
git commit -m "fix(#519,#522): the popup leads with what works, and the guide outlives the token"
```

---

### Task 4: Documentation

**Files:**
- Modify: `spec.md:2222-2231` (the two claims this change falsifies)
- Modify: `docs/extension-install-uk.md:240-248`
- Modify: `extension/CHANGELOG.md` (the `## [Unreleased]` list)

**Interfaces:**
- Consumes: the shipped behaviour from Tasks 1-3.
- Produces: nothing further.

- [ ] **Step 1: Fix the false claim in `spec.md`**

The current text ends the popup-cache bullet with:

> Поки токен **не заданий**, popup, **окрім** цього меню (кнопки лишаються), додатково показує «Not connected», кнопку «Get a token» і те саме посилання «Read the setup guide →»; після збереження токена ці три додаткові елементи ховаються.

Replace that sentence with:

> Поки токен **не заданий**, popup показує «Not connected» і кнопку «Get a token» одним блоком (`#authBlock`) **одразу під шапкою**, а «Sync my check-ins» демотується в secondary, стає `disabled` і несе підпис `Add a token to sync your check-ins.` — без токена синк не опитується взагалі, тож збережений з минулого разу `outcome: 'error'` не може перебити цей підпис. Після збереження токена блок ховається, а посилання «Read the setup guide →» **лишається видимим** — воно живе в підвалі перед «Clear all cache» і переїжджає в `#authBlock` лише коли токена немає (#519, #522).

- [ ] **Step 2: Fix the ordering claim in `spec.md`**

The «Popup — ієрархія подання» bullet opens with:

> порядок у DOM = порядок за важливістю — «Sync my check-ins» (primary, залитий акцентом) → «Refresh this page» (secondary, контур) → згорнутий список «Supported shops» → «Clear all cache» (ghost, під лінією-роздільником).

Replace that opening with:

> порядок у DOM = порядок за важливістю **для поточного стану**. З токеном: «Sync my check-ins» (primary, залитий акцентом) → «Refresh this page» (secondary, контур) → згорнутий список «Supported shops» → підвал із посиланням на гайд і «Clear all cache» (ghost, під лінією-роздільником). Без токена першим після шапки йде `#authBlock` («Not connected» + «Get a token» у primary + посилання на гайд), а «Sync my check-ins» опускається в secondary й `disabled`.

Leave the rest of that bullet (the shop list, the language pick, the running-sync note, the disabled tokens, the theme) untouched.

- [ ] **Step 3: Update `docs/extension-install-uk.md`**

Around line 243 the guide currently says the popup shows the «Not connected» note next to «Get a token» and the guide link, and that «Ці додаткові елементи зникають, щойно ти збережеш токен». Replace that passage with:

```markdown
Поки токен не збережено, popup ставить блок «Not connected» з кнопкою
**«Get a token»** одразу під заголовком — це і є перша дія, на яку потрапляє
`Tab`. Кнопка **«Sync my check-ins»** у цьому стані неактивна й підписана
`Add a token to sync your check-ins.`: без токена синхронізація все одно не
відбулася б, тож розширення не питає зайвого дозволу на доступ до untappd.com
і не показує помилку з'єднання.

Щойно ти збережеш токен, цей блок зникає, а «Sync my check-ins» знову стає
головною кнопкою. Посилання **«Read the setup guide →»** нікуди не дівається —
воно просто переїжджає вниз, у підвал popup'а, над «Clear all cache».
```

Check the surrounding sentences still read correctly (the section describes the popup top-to-bottom) and adjust the immediately adjacent wording if the replacement leaves a dangling reference.

- [ ] **Step 4: Add the release note**

At the end of the `## [Unreleased]` list in `extension/CHANGELOG.md`, add one entry in the file's established voice — user-facing, no issue numbers, no file names:

```markdown
- Before a token is saved, the popup now leads with getting one: «Get a token» sits directly under the title as the main button and is the keyboard's first stop, while «Sync my check-ins» is greyed out and explains that it needs a token — previously it was the big amber button, and pressing it asked for access to untappd.com only to fail with a connection error. The setup-guide link also stops vanishing once a token exists: it moves to the bottom of the popup and stays available for exactly the people who might hit a sync problem.
```

- [ ] **Step 5: Verify no other document repeats the old claim**

Run: `grep -rn "Get a token" docs/ spec.md extension/README.md 2>/dev/null`
Read each hit and confirm none still says the guide link disappears once a token is saved, or that «Sync my check-ins» is the popup's primary button unconditionally. Fix any that do.

- [ ] **Step 6: Run the full gate**

Run: `cd extension && npm test && npm run typecheck`
Then: `cd .. && npm test && npm run typecheck`
Expected: green (the root suite includes docs-shape tests that can react to `spec.md` edits).

- [ ] **Step 7: Commit**

```bash
git add spec.md docs/extension-install-uk.md extension/CHANGELOG.md
git commit -m "docs(#519,#522): the popup's hierarchy depends on the token, and the guide no longer vanishes"
```

---

## Self-Review

**Spec coverage:** decision 1 (disabled sync + caption) → Task 1 view model, Task 3 wiring; decision 2 (auth block moves after the header) → Task 1 applier + Task 2 wrapper; decision 3 (guide has one home per state, `guideLinkVisible` deleted) → Task 1 `guideHome`, Task 2 footer markup, Task 3 deletion; decision 4 (no poll without a token) → Task 3 Step 5 and its integration test; decision 5 (refusal to hide the cache actions) → Global Constraints, enforced by the absence of any task touching `#refresh` or `#clearAll`; documentation section → Task 4.

**Placeholders:** none — every code step carries the code, every mutation table names the test that must go red.

**Type consistency:** `TokenStateView` and `TokenStateNodes` are defined once in Task 1 and consumed verbatim in Task 3; `TokenStateNodes` includes `header`, which the spec's node list omitted — the applier needs it to insert after the header, and Task 3's call site supplies it from `document.querySelector('header.head')`.
